const { formatToPreviewUrl } = require('./src/utils/formatDriveUrl.ts');
// Note: we can't require TS directly if it uses ES modules or types.
// We can just run it using node if we remove the export and types.
